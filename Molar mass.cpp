#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t,i,j,n;
    string s;
    cin>>t;
    while(t--)
    {
        getline(cin,s);
        for(i=0;i<s.size();i++)
        {
            if(s[i]=='C'&&s[i+1]=)
        }
    }
}
